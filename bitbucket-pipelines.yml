image: node:alpine
definitions:
  caches:
    npm: ~/.npm
pipelines:
  default:
    - step:
        name: Validate code
        caches:
          - yarn
        script:
          - yarn install
          - cd ios && pod install
          - cd android && ./gradlew clean
          